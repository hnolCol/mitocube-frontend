## Animated Number 

Animates a numeric value (increasing) with a label.

```javascript
function AnimatedNumericMetrics({ number = 4, label = "", roundValue = true, roundPrecision = 0}) {
    return (
        ...
    )
    }
```