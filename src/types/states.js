/**
 * @typedef StatesResponse
 * @type {Object}
 * @property {Object.<string, Number>} states - The states by their name.
 * @property {Object.<string, string>} states_inv - The inverse of the states. The keys are always strings in js, hence transform state numbers (integers) first to string before getting the name or color.
 * @property {Object.<string, string>} colors - The colors of the states by their name.
 * @property {Object.<string, string>} colors_inv - Color by the state integer. Note that keys in js are always string. 
 */


export default {}